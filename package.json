{
	"name": "ohkami-yew-todo",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --assets dist",
		"dev": "wrangler dev --env dev --port 8787"
	},
	"devDependencies": {
		"wrangler": "^3.50"
	}
}
